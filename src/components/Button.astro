---
interface Props {
    title:string
    link?:string
}

const{title, link} = Astro.props
---

<button class="px-12 py-2 text-xl text-white font-semibold rounded-full bg-orange-400 hover:bg-orange-700 focus:ring-2  focus:ring-white md:px-20 md:text-2xl"><a href={link}></a>{title}</button>